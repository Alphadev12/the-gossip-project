<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<a class="navbar-brand" href="/gossips">The Gossip Project</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
	<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="/team/show">Team</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="/contact/show">Contact</a>
			</li>
		</ul>
	</div>
</nav>
<div>
	<h1 class="text-center m-4"><%= @city.name %></h1>
	<div class="row text-center">
		
		<% @users.each do |user| %>
		<div class="col-6">
			<ul>
				<h3 class="text-success"> <%= user.first_name %> </h3>

				<% gossips = user.gossips %>
				<% gossips.each do |gossip| %>
					<li> <%= gossip.title %> </li>
				<% end %>
			</ul>
		</div>
		<% end %>
	</div>
</div>