<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<a class="navbar-brand" href="/">The Gossip Project</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
	<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="/team/show">Team</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="/contact/show">Contact</a>
			</li>
		</ul>
	</div>
</nav>

<div class="text-center mt-4">
	<h1> <span class="text-info"> <%= @gossip.title %> </span> de <a href="/user/<%= @gossip.user.id %>"><%= @gossip.user.first_name %></a> </h1>

	<div class="display-4">
		<%= @gossip.content %>
	</div>

	<table class="table table-bordered">
		
	</table>
	
	<p> 
		<a href="/city/<%= @gossip.user.city.id %>"><%= @gossip.user.city.name %> </a>
	</p>
	
	<p> <%= @gossip.created_at %> </p>
</div>

